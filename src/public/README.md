# Deprecated

This folder contains redundant assets (fonts and icons) that are already managed in the project root's `fonts/` and `icons/` directories. There are no active code references to this path in the build system.

**Status:** Unused/Redundant.
