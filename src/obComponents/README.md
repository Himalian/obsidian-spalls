# Deprecated

This folder contains logic that directly interacts with the Obsidian API. Despite the "Components" suffix in the folder name, most of these are business logic services (CRUD operations).

**Status:** Logical chaos.
**Refactoring recommendation:** This logic should be moved to `src/services/` or `src/data/` to separate it from actual UI components. Do not add new logic here.
